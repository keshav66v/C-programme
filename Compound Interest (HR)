#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {  
    int P;
    scanf("%d",&P);
    float R;
    scanf("%f",&R);
    int T;
    scanf("%d",&T);
    float CI;
    CI = P * (pow(1+R / 100 , T)- 1);
    printf("%.2f",CI);
    return 0;
}
