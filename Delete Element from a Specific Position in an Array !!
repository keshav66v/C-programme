#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,pos;
    scanf("%d",&n);
    int arr[n];
    for(int i=0 ; i<n; i++){
        scanf("%d", &arr[i]);
    }
    scanf("%d", &pos);
    if(pos>n)
        printf("Invalid position");
    else{
        for(int i= pos; i<n; i++){
            arr[i-1] = arr[i];
        }
        n--;

    for(int i=0; i<n; i++){
          printf("%d ", arr[i]);
    }
    }
    return 0;
}
