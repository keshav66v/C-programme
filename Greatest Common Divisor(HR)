#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>


int main() {

    int A,B;

    scanf("%d %d", &A, &B);

    int temp;

    while (B != 0) {

        temp = B;

        B = A % B;

        A = temp;

    }

    printf("%d\n", A);

    return 0;

}
