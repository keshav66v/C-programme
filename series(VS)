#include<stdio.h>
#include<Math.h>
int main(){
    int i,n,ans;
    printf("Enter the value of n = ");
    scanf("%d",&n);
    for(i=1;i<=n;i++){
        ans =(int)pow(i,2);
        printf("%d\n",ans);
    }
    return 0;
}
